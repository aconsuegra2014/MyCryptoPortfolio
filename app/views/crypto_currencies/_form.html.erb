<%= form_with(model: crypto_currency) do |form| %>
  <% if crypto_currency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crypto_currency.errors.count, "error") %> prohibited this crypto_currency from being saved:</h2>

      <ul>
        <% crypto_currency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :ticker_symbol %>
    <%= form.text_field :ticker_symbol %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
